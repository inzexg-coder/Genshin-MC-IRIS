package net.teyvat.combat;

/**
 * Комбо атак путешественника (Итэр/Люмин) с обычным мечом, как в Genshin:
 * серия из 5 ударов, у каждого своя длительность, момент нанесения урона
 * и множитель урона от атаки. Общие данные для клиента (тайминги анимации)
 * и сервера (множители урона).
 *
 * Удары (анимации — scripts/gen_genshin_combo.py):
 *   1. широкий горизонтальный удар слева направо;
 *   2. диагональный удар снизу справа вверх налево;
 *   3. удар с разворотом по кругу — клинок описывает круг справа сверху
 *      влево вниз (корпус делает большой поворот);
 *   4. горизонтальный удар справа налево;
 *   5. очень широкий удар справа налево с замахом за спину и прокатом вперёд.
 */
public final class SwordCombo {
    /** Число ударов в комбо (у путешественника с мечом — 5). */
    public static final int HIT_COUNT = 5;
    /** Длительность каждого удара в тиках (полный круг ≈ 2 сек — плотная
     *  серия без пауз: удары 1-2 = 0.35 с, удар 3-разворот = 0.45 с,
     *  удар 4 = 0.35 с, финальный широкий с прокатом = 0.5 с). */
    public static final int[] DURATION_TICKS = {7, 7, 9, 7, 10};
    /** Тик удара, в который наносится урон: середина быстрого сегмента клипа
     *  (см. scripts/gen_genshin_combo.py): удар 1 — пролёт клинка вправо,
     *  удар 2 — конец диагонали вверху слева, удар 3 — завершение круга
     *  внизу слева, удар 4 — пролёт клинка влево, удар 5 — очень широкий
     *  разрез через всё пространство перед героем. */
    public static final int[] DAMAGE_TICKS = {3, 3, 7, 4, 4};
    /** Множители урона от атаки: 44.7% / 43.4% / 53.0% / 58.3% / 70.9% (уровень таланта 1). */
    public static final float[] MULTIPLIERS = {0.447f, 0.434f, 0.530f, 0.583f, 0.709f};
    /** Пауза без нажатий, после которой комбо сбрасывается (2.5 сек). */
    public static final int RESET_TICKS = 50;
    /** Тик, начиная с которого нажатие (или удержание) ЛКМ цепляет следующий удар. */
    public static final int CHAIN_INPUT_TICKS = 2;
    /** Шаг героя вперёд на момент удара (блоки): герой не стоит на месте,
     *  а проталкивается в направлении взгляда, как в Genshin. Пятый удар —
     *  заметный прокат вперёд. */
    public static final float[] LUNGE_STRENGTH = {0.1f, 0.1f, 0.15f, 0.1f, 0.6f};
    /** Вертикальный подброс на ударе (пятый удар — низкий прокат, без прыжка). */
    public static final float[] LUNGE_UP = {0f, 0f, 0f, 0f, 0f};
    /** Отбрасывание цели по ударам (сервер): финальные удары бьют сильнее. */
    public static final float[] KNOCKBACK = {0.25f, 0.25f, 0.35f, 0.55f, 0.5f};

    private SwordCombo() {}

    /** Прогресс удара 0..1 (для интерполяции поз). */
    public static float progress(int hitTicks, int hitIndex) {
        return Math.max(0f, Math.min(1f, (float) hitTicks / DURATION_TICKS[hitIndex]));
    }
}
